h = 0 # h считает числа больше предыдущего, задаем первое и второе с помощью инт пока не 0 запись идет, если нынешнее число больше предыдущего то h + 1, задаем след число, вывод это числа больше предыдущего 8
n = int(input())
d = int(input())
while d != 0:
    if d > n:
        h += 1
    n = d
    d = int(input())
print(h)
